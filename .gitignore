# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# Virtual environment
D-FINE/venv/

# Large model files (download separately)
D-FINE/output/
D-FINE/weight/
dfine_app_server/models/

# Dataset (too large)
D-FINE/data/

# Offline-tiled SAHI dataset — regenerated by tools/tiling/build_tiled_visdrone.py,
# not committed (see experiments/sahi_tiled/)
dataset/visdrone_tiled/

# Flutter build artifacts
dfine_app/build/
dfine_app/.dart_tool/
dfine_app/.flutter-plugins
dfine_app/.flutter-plugins-dependencies

# D-FINE is its own repo — track as submodule, not inline
D-FINE/

# Logs
*.log
nohup.out
